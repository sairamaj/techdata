- 
    name: save.variable
    command: var json = JSON.parse(responseBody);
             postman.setEnvironmentVariable("AccessToken", json.access_token);
    description: Save the variable from response

